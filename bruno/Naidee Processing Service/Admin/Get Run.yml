info:
  name: Get Run
  type: http
  seq: 2

http:
  method: GET
  url: "{{Base_URL}}/admin/runs/{{Run_Id}}"
  headers:
    - name: x-api-key
      value: "{{Admin_Api_Key}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
